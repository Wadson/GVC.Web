document.getElementById('sidebarToggle')?.addEventListener('click',()=>{const sidebar=document.getElementById('sidebar');innerWidth<=768?sidebar.classList.toggle('open'):sidebar.classList.toggle('collapsed')});

window.gvcClienteForm=()=>{const tipo=document.getElementById('tipoPessoa');if(!tipo)return;const update=()=>{const pj=tipo.value==='PJ';document.querySelectorAll('.pf-field').forEach(x=>x.classList.toggle('d-none',pj));document.querySelectorAll('.pj-field').forEach(x=>x.classList.toggle('d-none',!pj));document.querySelectorAll('.document-mask').forEach(x=>x.disabled=true);const current=document.querySelector(`.${pj?'pj':'pf'}-field .document-mask`);if(current)current.disabled=false};tipo.addEventListener('change',update);update()};

window.gvcKeyboardList=(input,container,onSelect)=>{let index=-1;const buttons=()=>[...container.querySelectorAll('button:not([disabled])')];const paint=items=>{items.forEach((item,i)=>item.classList.toggle('active',i===index));if(index>=0)items[index].scrollIntoView({block:'nearest'})};input.addEventListener('keydown',event=>{const items=buttons();if(!items.length)return;if(event.key==='ArrowDown'){event.preventDefault();index=(index+1)%items.length;paint(items)}else if(event.key==='ArrowUp'){event.preventDefault();index=index<=0?items.length-1:index-1;paint(items)}else if(event.key==='Enter'&&index>=0){event.preventDefault();onSelect(items[index]);index=-1}});return()=>{index=-1}};

window.gvcCitySearch=()=>document.querySelectorAll('[data-city-search]').forEach(input=>{if(input.dataset.ready)return;input.dataset.ready='1';const hidden=document.getElementById(input.dataset.cityTarget),results=input.parentElement.querySelector('[data-city-results]');let timer;const choose=button=>{hidden.value=button.dataset.id;input.value=button.dataset.label;results.innerHTML='';reset()};const reset=window.gvcKeyboardList(input,results,choose);input.addEventListener('input',()=>{hidden.value='';clearTimeout(timer);results.innerHTML='';reset();if(input.value.trim().length<2)return;timer=setTimeout(async()=>{const response=await fetch(`/Cidades/Buscar?termo=${encodeURIComponent(input.value.trim())}`);if(!response.ok)return;const cities=await response.json();results.innerHTML=cities.map(x=>`<button type="button" class="list-group-item list-group-item-action" data-id="${x.id}" data-label="${x.nome} - ${x.uf}">${x.id} - ${x.nome} - ${x.uf}</button>`).join('')},250)});results.addEventListener('click',event=>{const button=event.target.closest('button');if(button)choose(button)});document.addEventListener('click',event=>{if(!input.parentElement.contains(event.target)){results.innerHTML='';reset()}})});
window.gvcCitySearch();

window.gvcImagePreview=()=>document.querySelectorAll('[data-image-input]').forEach(input=>{if(input.dataset.ready)return;input.dataset.ready='1';input.addEventListener('change',()=>{const preview=document.getElementById(input.dataset.imagePreview),file=input.files?.[0];if(!preview||!file)return;const reader=new FileReader();reader.addEventListener('load',()=>{preview.src=reader.result;preview.classList.remove('d-none')});reader.readAsDataURL(file)})});
window.gvcImagePreview();

window.gvcTableSearch=()=>{const pageHasSearch=!!document.querySelector('form[method="get"]');document.querySelectorAll('table').forEach((table,tableIndex)=>{if(table.dataset.searchReady||table.querySelector('#itensVenda,#parcelasVenda')||table.closest('.modal'))return;const tbody=table.tBodies[0];if(!tbody)return;const rows=[...tbody.rows].filter(row=>!row.querySelector('td[colspan]'));if(!rows.length)return;table.dataset.searchReady='1';let input=pageHasSearch?document.querySelector('form[method="get"] input:not([type="hidden"]):not([type="date"]):not([type="number"])'):null;if(!input){const wrapper=document.createElement('div');wrapper.className='input-group mb-3';wrapper.innerHTML='<span class="input-group-text"><i class="bi bi-search"></i></span><input type="search" class="form-control" placeholder="Pesquisar por nome ou código" autocomplete="off">';const host=table.closest('.card')||table;host.parentElement.insertBefore(wrapper,host);input=wrapper.querySelector('input');input.addEventListener('input',()=>{const term=input.value.trim().toLocaleLowerCase('pt-BR');rows.forEach(row=>{const extra=[...row.querySelectorAll('a[href],form[action]')].map(x=>x.getAttribute('href')||x.getAttribute('action')||'').join(' ');row.hidden=term!==''&&!`${row.innerText} ${extra}`.toLocaleLowerCase('pt-BR').includes(term)});selected=-1;paint()})}if(input.dataset.tableKeyboard)return;input.dataset.tableKeyboard='1';let selected=-1;const visible=()=>rows.filter(row=>!row.hidden);const paint=()=>rows.forEach(row=>row.classList.remove('table-primary'));input.addEventListener('keydown',event=>{const items=visible();if(!items.length)return;if(event.key==='ArrowDown'){event.preventDefault();selected=(selected+1)%items.length}else if(event.key==='ArrowUp'){event.preventDefault();selected=selected<=0?items.length-1:selected-1}else if(event.key==='Enter'&&selected>=0){const link=items[selected].querySelector('a[href]');if(link){event.preventDefault();link.click()}return}else return;paint();items[selected].classList.add('table-primary');items[selected].scrollIntoView({block:'nearest'})})})};
window.gvcTableSearch();

document.addEventListener('DOMContentLoaded',()=>{if(window.jQuery?.validator)jQuery.validator.methods.number=value=>value===''||/^-?(?:\d+|\d{1,3}(?:\.\d{3})+)(?:,\d+)?$/.test(value)||/^-?\d+(?:\.\d+)?$/.test(value)});
